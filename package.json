{
  "name": "video-factory",
  "version": "0.0.1",
  "private": true,
  "description": "Enterprise Agent video production experiment, orchestrated by Trigger.dev",
  "type": "module",
  "engines": {
    "node": ">=20.0"
  },
  "scripts": {
    "dev": "trigger dev",
    "build": "trigger build",
    "deploy": "trigger deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@trigger.dev/sdk": "^4.6.2",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}